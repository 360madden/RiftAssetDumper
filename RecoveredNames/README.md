# Recovered names

This folder is reserved for durable recovered-name artifacts such as:

```text
recovered-names.jsonl
```

Records should be produced by `match-names` only when candidate paths have hash/length/confidence evidence. Do not place copied game payloads here.
