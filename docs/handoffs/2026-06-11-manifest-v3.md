# Session Handoff — 2026-06-11: Export Manifest v3 (SHA256 + Live Provenance)

**Commit:** `3f521b1` — `feat: extend export manifest with SHA256 hashing and live-archive provenance detection`

## What Changed

Extended `scripts/build_export_manifest.py` (schema v2 → v3):

| Feature | Details |
|---------|---------|
| **SHA256 hashing** | Every OBJ now has a content hash in the manifest for integrity verification |
| **Live provenance** | Detects live-archive vs copied-source OBJs via known live-exported asset IDs |
| **Provenance breakdown** | Summary stats show live vs copied counts |
| **Schema upgrade** | v2 → v3 to reflect new `sha256` and `provenance` fields |

## Manifest Results (348 OBJs)

| Metric | Value |
|--------|:-----:|
| Total OBJs | 348 |
| Copied-source | 345 |
| Live-archive | **3** (362, 357, 349) |
| Faced | — |
| Unknown MeshSize entries | 39 |

## Bug Fixed

`classify_export_batch` function was accidentally absorbed into `detect_provenance` during str_replace — restored as separate function with correct scoping.

## CI

Ruff ✅ Mypy ✅

## Next Steps

The manifest is now production-ready with integrity hashing. The live provenance detection should be upgraded from hardcoded IDs to a data-driven approach (read from live-family-scanner output).
