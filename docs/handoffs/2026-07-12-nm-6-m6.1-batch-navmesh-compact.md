# Compact Handoff — 2026-07-12 (NM-6 M6.1 Batch Navmesh)

> **Resolved by follow-up:** M6.1 hardening and M6.2-M6.4 are complete. See
> `2026-07-12-nm-6-phase6-completion.md` for current truth.
>
> TL;DR companion to the full handoff
> [`2026-07-12-nm-6-m6.1-batch-navmesh.md`](2026-07-12-nm-6-m6.1-batch-navmesh.md).
> Read this for fast context resumption; read the full doc for design rationale.

## Historical ship status

**IMPLEMENTATION SHIPPED; HARDENING OPEN** — commit `9473143` — CI run
`29155486795` green (5/5 jobs)

- `ruff` clean, `mypy` clean
- Local `pytest` project-wide: **1227 passed + 3 deliberate skips** (zero failures)
- Restricted real-zone smoke: `BUILT (9 polys, 9 walkable)` on
  `ep1.world_objects.dungeons`
- Schema self-check: passes (11 zones locked in `navmesh-index-v1`)

Post-ship audit correction (2026-07-12): normal CI does not run the complete
`tests/` pytest suite. The canonical index was written by the restricted
`--zones ep1.world_objects.dungeons` smoke. Current inputs contain four zones
meeting the default threshold, but the stored index contains only the selected
dungeon cohort and does not record selection scope or input provenance. Treat
the stored index as smoke evidence, not a full-batch inventory.

## Files

| File | Role |
|---|---|
| `scripts/build_all_navmeshes.py` (781 lines, new) | Subprocess-orchestrated batch with `run` / `status` / `check-schema` |
| `docs/schemas/navmesh-index-v1.schema.json` (190 lines, new) | JSON Schema (DRAFT-07): 5-label + 3-reason enums, `connected_zones` regex, `failure` shape |
| `tests/test_build_all_navmeshes.py` (768 lines, new) | 29 cases: 26 pass + 3 deliberate skips |

## CLI quickstart

```bash
python scripts/build_all_navmeshes.py run                                 # full batch
python scripts/build_all_navmeshes.py run --zones ep1.world_objects.dungeons  # NM-1 pilot
python scripts/build_all_navmeshes.py run --min-walkable 3                 # relaxed threshold
python scripts/build_all_navmeshes.py status                              # on-disk index summary
python scripts/build_all_navmeshes.py check-schema                        # cheap pre-merge gate
```

## Key design (load-bearing only)

- **Subprocess orchestration** (not direct import) — mirrors FT-2 pattern; per-zone fail-isolation.
- **Eligible-zone filter** — `walkability.label ∩ flythrough-index.zone.tuple`, ≥5 walkable assets.
- **`__all__` re-export** — `subprocess` declared so `monkeypatch.setattr(bam.subprocess, "run", ...)` works.
- **Loader-default hardening still required** — committed loaders capture
  `FLY_INDEX`/`WALK_PATH` at function-definition time. Convert them to
  `Path | None = None` with call-time resolution.

## Next milestone

**M6.1 hardening and an unfiltered full-batch run**, then M6.2 zone-graph
adjacency. The schema pre-wires `zones.<tuple>.connected_zones`; the full
handoff retains the M6.2 implementation sketch.

## Known debt

Three `@pytest.mark.skip` markers in `tests/test_build_all_navmeshes.py`:

- `test_main_run_no_eligible_zones_writes_empty_index` — exposes the committed
  def-time loader defaults.
- Two `test_load_walkability_empty_on_*_logs_warning` tests read `capsys`
  before invoking the warning-producing loader.

The proven dungeon path is correct, but these skips are avoidable and the full
batch remains unproven. Fix them in the M6.1 hardening pass. Also add
`python -m pytest tests/` to normal CI; the current workflow runs standalone
`scripts/test_*.py` files plus four orphan-guard pytest files only.

## Cycle context

NM-6 (current cycle): M6.1 implementation shipped; hardening/full-batch proof
remains — **M6.2 / M6.3 / M6.4 / NM-7 remain.**
Prior: NM-0 → NM-5 ✅; Cycle 5 ✅ (C5-a Tier-1 archive + C5-b semantic surface); Cycle 4 ✅ (mesh297/321); Cycle 2 ✅ (scene manifests, 153/217 consumer_ready).

## Anti-drift guardrails (kept)

- No new C# parse logic — M6.1 calls into existing recast4j pipeline via `build_navmesh.py`.
- `.nav` / `.bin` navmesh files are gitignored (generative artifacts).
- No new cross-repo edits — RiftFlythrough consumer unchanged.
- `__all__` keeps clean public surface; private helpers still exposed for tests.
