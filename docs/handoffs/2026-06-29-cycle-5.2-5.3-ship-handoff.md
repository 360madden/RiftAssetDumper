# Session-end followup handoff — cycle 5.2 + 5.3 ships + GH Issue #1

- **Date:** 2026-06-29 (session-end followup)
- **Authors:** Buffy orchestration (Corbin → 360madden)
- **Scope:** Resumption context for the next session
- **Status baseline:** Cycle 5.2 + 5.3 SHIPPED to `origin/main`; GH Issue #1 filed

---

## 1. Headline state

| Item | Value |
|---|---|
| Branch | `main` |
| HEAD (local) | `ef75edfcf948ce61540c03a16926e50175f2f891` |
| HEAD (origin/main) | `ef75edfcf948ce61540c03a16926e50175f2f891` |
| Divergence | 0 ahead / 0 behind (fully synced) |
| Working tree | clean (`git status -sb` empty for tracked files) |
| Untracked files | 4 abandoned WIP scanner files from cycle-5.4-pivot deferral (`scripts/{modrm_scanner,signature_match}.py`, `tests/test_{modrm_scanner,signature_match}.py`) — NOT shipped in cycle-5.3.2 follow-up per project convention for handoff-marked follow-up commits |
| `lint.out` debug artifact | absent (deleted earlier this session) |
| GH Issue #1 | filed — *Enable branch protection on `main`: require CI green before merge*; PR #2 roundtrip has now landed green, so P5 activation is safely unblocked |

> **★ Recommended first action on resume:**
>
> - **If you have repo admin:** start with **P2** (apply/create the 3 missing labels) — 5-minute housekeeping with zero code risk.
> - **Else if any F1–F5 feels trivial** (typically F5 is a one-line JSON escape add): pick **P3** and bundle 2–3 follow-ups into a `cycle-5.3.1-polish` commit.
> - **Else defer to next substantive cycle** (5.3.1 or 6.0) and start with **P4** (begin **M1.6+** post-M1.5 phase-1-exit on `binary-signature-roadmap.md` once Ghidra execution is ready).
> - **Do not enable P1** (branch protection rule) until at least one Option-B roundtrip has landed green — see [D3] rationale.

### Last 3 commits this session shipped

| SHA | Subject |
|---|---|
| `ef75edf` | `cycle-5.3.2: defer F4 review nits — cancellation propagation + per-hit fold` |
| `4f1d7f0` | `docs: clarify canonical Option-B contribution path in README (#2)` |
| `55fbdc0` | `cycle-5.3.1-polish: bundle F1-F5 reviewer follow-ups` |

> **Lineage anchor:** `a2bcc8c` (cycle-5.2 zone-confidence ship) is the baseline commit
> for this audit lineage. Run `git log --oneline origin/main -n 12` for full history
> including `69ae7ee` and `7b7c733`.

### What landed in cycle 5.2 v0.6 (`a2bcc8c` + `7b7c733`)

- `scripts/build_riftflythrough_delivery.py` — producer emits **unprefixed** `first4`/`confidence` (was `zone_first4`/`zone_confidence`)
- `tests/test_build_riftflythrough_delivery.py` — 2 lockdown tests pinning the uniform-substring rename
- `docs/schemas/scene-manifest-v1.schema.json` — description rewritten with scope clarification
- `docs/handoffs/2026-06-28-cycle-5.2-zone-confidence.md` — flipped to `SHIPPED`
- `knowledge.md` — uniform substring rename
- 3 review-fix hedges (`7b7c733` only):
  - `docs/handoffs/2026-06-28-session-handoff.md` — softened `ModRM-based byte scanning found 1,337 hits = definitive proof` → `manual scan finding (not yet validated by automated tool)` in 4 places
  - `docs/roadmap/binary-signature-roadmap.md` — L5 status + 6 Phase-N checklist markers prefixed `*(as of 2026-06-28; self-reported; not yet externally validated)*`
  - `docs/roadmap/semantic-discovery-roadmap.md` — L5 status + 6 Phase-N checklist markers prefixed similarly

### What landed in cycle 5.3 audit (`69ae7ee`)

6 untracked files ship'd (1 doc, 4 utilities, 1 doc only): all have 1+ cross-references in existing project docs.

| File | Lines | Purpose | Cross-ref |
|---|--:|---|---|
| `docs/handoffs/2026-06-28-phase0-dual-exit.md` | 214 | Documents Phase 0 dual exit (semantic + binary roadmaps M0.1–M0.4) | ⚠ unverified — searches found no in-tree cite outside this handoff + cycle-5.3-audit commit message; grep on resume to confirm |
| `scripts/ghidra/DisasmContext.java` | 130 | Disassembles ±40 instructions around target addresses, JSON output | `binary-signature-roadmap.md` L162, L167 |
| `scripts/ghidra/ScalarOffsetSearcher.java` | 267 | Finds scalar operands matching target offsets (0x304–0x328), JSON output | `binary-signature-roadmap.md` L161, L168 |
| `scripts/ghidra/VtableResolver.java` | 124 | Reads 64-bit vtable pointers, JSON output | `binary-signature-roadmap.md` M1.7 (vtable-resolution step — section anchor survives edits) |
| `scripts/parse_map_blobs.py` | 260 | Stdlib-only decoder for short int / length-prefixed string / protobuf-like blobs | `2026-06-28-session-handoff.md` L114 (mypy ✅) |
| `scripts/poll_ci_v1.sh` | 157 | Bash utility mirroring root `poll_ci_v1.sh`; LF-cleaned (was CRLF) | `2026-06-13-stale-data-cleanup-handoff.md` L148 |

### GH Issue #1 — branch protection rule spec

- **Issue URL:** `https://github.com/360madden/RiftAssetDumper/issues/1`
- **Title:** *Enable branch protection on `main`: require CI green before merge*
- **Labels:** none applied yet (repo lacks `infrastructure` / `ci-gating` / `priority:medium` — `--label` flags dropped on retry)
- **Required checks** (in GH Issue body): `Docs Lint`, `.NET Build & Test`, `Python Lint & Test`, `Orphan Guard Regression`, `Summary`
- **Rule spec:** PR gate on `main`, no force-push, no admin bypass (admin still can if absolutely needed)
- **Rollout gating (critical):** the issue body specifies that **the rule cannot be activated until at least one Option-B roundtrip has landed green** — otherwise it would block Buf's own workflow

---

## 2. Decisions made this session

All three ship decisions were direct-push-to-main, not Option-B (feature-branch + PR). Rationale:

| # | Decision | Why | Risk | Mitigation |
|---|---|---|---|---|
| D1 | Ship `7b7c733` direct-push (skip Option B) | Terse user instruction ("ccommit"); pre-validated locally (lint, ruff, mypy, py_compile, bash -n, dotnet format); 0 known regressions | No CI gate at land | GH Issue #1 filed for branch-protection rule (gated on Option-B roundtrip) |
| D2 | Ship `69ae7ee` direct-push (skip Option B) | Same as D1; audit-ship is a sequencing commit, not a feature commit | Same | Same |
| D3 | Apply branch-protection rule gated on first Option-B roundtrip | Rule activated before Option-B works → blocks our own workflow | Rule-stays-off window leaves direct-push exposed | Issue body documents this gating explicitly; admin can disable rollout gate once Option-B is proven |

### Bugs caught + fixed during ship-prep (not deferred)

1. **`scripts/poll_ci_v1.sh`** had **157 CRLF line terminators** (would break Linux CI; `bash -n` ran on Windows which masked this). Fix: in-place CRLF→LF rewrite before staging. Caught by `file scripts/poll_ci_v1.sh` after `bash -n` falsely passed.
2. **`docs/handoffs/2026-06-28-phase0-dual-exit.md`** had **2 MD031 errors** (fenced code blocks at L230/L242 lacked blank lines around them). Fix: blanket-blank-line insert via `sed`-style pass (blank above each opening fence, blank below each closing fence, dedup consecutive blanks). Caught by `markdownlint-cli2 ./*.md` before staging.

Both fixes are mentioned in the `69ae7ee` commit body for audit traceability.

---

## 3. Pending actions table

| # | Owner | Action | Status | Blocker / Next step |
|--:|---|---|---|---|
| P1 | Repo admin | Enable branch protection on `main` per GH Issue #1 spec | **DONE** (activated 2026-07-09 via `gh api`; 5 required checks live) | — |
| P2 | Repo admin | Create + apply `infrastructure`, `ci-gating`, `priority:medium` labels on Issue #1 | Not done | Pre-check: `gh label list 2>&1 \| grep -E 'infrastructure\|ci-gating\|priority:medium'` — only retry with `--label` flags after a label is confirmed to exist on the repo |
| P3 | Next session | Reviewer follow-ups from cycle 5.3 audit (5 items — see §5) | Open | All minor / non-blocking; bundle if convenient |
| P4 | Next session | Begin Phase 1 step 6+ on `binary-signature-roadmap.md` (1,337 candidate sites → signature extraction) | Self-reported; not yet externally validated | Ghidra execution needed before any commitment |
| P5 | Next session | Cross-map the 3 cycle-5.3-audit `.java` scripts against `FunctionSiteSurvey.java` capability surface | New (raised by reviewer) | Avoid duplicated logic in future Ghidra work |
| P6 | Any session | Build evidence lane for asset-guided runtime re-acquisition strategy (`docs/asset-guided-runtime-reacquisition-strategy.md`) | Not started | Foundation doc is lint-clean but evidence work isn't fired |

---

## 4. Resume protocol (cold-start)

When resuming this project from a clean checkout or after a session break:

1. **Verify clean state:**

   ```bash
   cd "C:/RIFT MODDING/Assets"
   git status -sb                        # expect: clean, on main, 0 divergence
   git rev-parse HEAD                    # expect: 69ae7ee
   git log --oneline -n 1 origin/main    # expect: 69ae7ee
   ```

2. **Read in this order:**

   1. `docs/handoffs/2026-06-29-cycle-5.2-5.3-ship-handoff.md` (**THIS doc**)
   2. `docs/handoffs/2026-06-28-session-handoff.md` — binary-signature detail
   3. `docs/handoffs/2026-06-28-cycle-5.2-zone-confidence.md` — zoning changes
   4. `docs/handoffs/2026-06-28-phase0-dual-exit.md` — phase 0 dual exit (cycle 5.3 audit item)
   5. `docs/roadmap/binary-signature-roadmap.md` — long-term plan (M0–M1.8+)
   6. `docs/roadmap/semantic-discovery-roadmap.md` — long-term plan (M0–M1.8+)
   7. `docs/asset-guided-runtime-reacquisition-strategy.md` — foundation doc for P6
   8. GitHub Issue #1 (via web) — branch-protection rule spec + rollout gating

3. **Pick a pending action** from §3 (any that is owner = "next session" or "any session") and proceed.

4. **For each commit**, follow project conventions (§6) — especially CRLF→LF on .sh/.bat, MD031 fence-blanks on .md, label existence pre-check before `--label`.

---

## 5. Reviewer follow-ups (non-blocking)

From the cycle 5.3 audit code-reviewer (`69ae7ee`). Not addressable before push because they're stylistic / defensive. Recommend bundling 2–3 of these into a single cycle-5.3.1 commit when convenient.

| # | File | Follow-up | Severity |
|--:|---|---|---|
| F1 | `scripts/poll_ci_v1.sh` | 2× `sed` calls in owner/name extraction can be 1 | low (cosmetic) |
| F2 | `scripts/parse_map_blobs.py` | `_decode_length_prefixed_strings` + `_decode_protobuf_like` lack infinite-loop guards; add `MAX_RECORDS` cap | medium (defensive — small input today, no observable bug yet) |
| F3 | `docs/handoffs/2026-06-28-phase0-dual-exit.md` | CI-Health-Sweep table has no stdout excerpts → "ruff/mypy/dotnet pass" claim is unverifiable | low (verify in next session) |
| F4 | `scripts/ghidra/ScalarOffsetSearcher.java` | Silently swallows exceptions in prev/next context walk; surface as warning | SHIPPED in cycle-5.3.2 follow-up (3 cancel-narrowings + per-hit folded summary + 10-test regex-proximity fixture) |
| F5 | `scripts/ghidra/DisasmContext.java` | `escapeJson` only handles 4 chars (`\"`, `\\`, `\n`, `\r`) — missing `\b`, `\f`, `\u00XX` | low (Ghidra instructions are ASCII; moot in practice) |

---

## 6. Project conventions reaffirmed (this session)

- **One consolidating commit per cycle** — no atomic per-file commits (the cycle 5.3 audit ship is the canonical example: one `69ae7ee` for 6 files).
- **Direct push to `main` accepted for handoff-marked cycles** — no PR required (matches Round-trip pattern documented pre-2026).
- **`.gitignore` working-tree clean before each commit** — `git status -sb` empty.
- **`.sh` / `.bat` files: CRLF→LF normalization required** before commit (run a `python -c "open(p,'rb').read().replace(b'\r\n', b'\n')"` pass during ship-prep).
- **Markdown fenced code blocks: insert blank lines above + below** per MD031.
- **`--label` only after confirming label exists on the repo** — if unsure, omit the flag and let the GH UI label it later.
- **Hedging convention for self-reported status** — when shipping unverified claims, prefix with `*(as of YYYY-MM-DD; self-reported; not yet externally validated)*`. This is the lingua franca for cycle-5.2 v0.6 hedges.
- **Commit prefix convention** — `feat(cycle-N.X):` for cycle work, `docs:` for handoffs, `fix:` for bugfixes, `test:` for test-only changes. Per `knowledge.md`.

---

## 7. Tools / paths quick-ref

| Use | Path / Command |
|---|---|
| C# build | `dotnet build RiftAssetDumper.slnx --nologo` |
| C# test | `dotnet test RiftAssetDumper.slnx --nologo` (56 tests) |
| C# format | `dotnet format RiftAssetDumper.slnx --verify-no-changes` |
| Python orchestrator | `python scripts/rift_workflow.py <cmd>` |
| Python read-only (no dotnet spawn) | `python scripts/rift_read_only.py <cmd>` |
| Python lint | `python -m ruff check scripts/` |
| Python type-check | `python -m mypy scripts/ --no-error-summary` |
| Python test | `pytest tests/ scripts/` (593 tests) |
| Markdown lint | `C:\RIFT MODDING\Tools\markdownlint-cli2\markdownlint-cli2.cmd <file>` |
| Ghidra | `C:\RIFT MODDING\Tools\ghidra_12.1_PUBLIC\support\analyzeHeadless.bat` |
| Pre-commit hook | `.pre-commit-config.yaml` (1 fingerprint guard per knowledge.md) |
| Live game path (read-only) | `C:\Program Files (x86)\Glyph\Games\RIFT\Live` (26GB, 244 archives, 263,957 entries) |
| Sibling RiftFlythrough deliverable | `C:\RIFT MODDING\RiftFlythrough\js\riftflythrough-delivery.json` (153/217 consumer-ready) |

---

## 8. Risks & gotchas

1. **`scripts/poll_ci_v1.sh` (root)** and **`scripts/poll_ci_v1.sh`** mirror each other's semantics — they are intentionally two copies (one for direct use, one for the project root). Do not de-duplicate.
2. **Ghidra scripts** (`scripts/ghidra/*.java`) run inside `analyzeHeadless.bat` against the `rift_x64.exe` binary, NOT against any source file. They import `ghidra.app.script.GhidraScript` and use `currentProgram`. They produce file-system JSON output.
3. **The 1,337 ModRM scan hits** are still a **manual finding** — no shipping automated scanner (per Finding #1 from cycle 5.2 review). Any quantitative claim based on this number must be hedged.
4. **Direct push to `main`** for handoff-marked cycles remains the convention for now; the GH Issue #1 branch-protection rule is gated on first Option-B roundtrip. Once that roundtrip lands, activate the rule and direct-push becomes a documented fallback (rare cases).
5. **`Synthesize_semantic_matrices.py` audit-key correctness finding** still applies: `entry.get("ArchiveProvenance")` always returns `None` (it's a `NamedTuple` destructured into `ArchiveName` + `EntryIndex` per `docs/schemas/asset-semantic-index-v1.schema.json`). Use the on-disk schema fields.
6. **`Source/` is permanently deleted** — all scripts read live from `C:\Program Files (x86)\Glyph\Games\RIFT\Live` (read-only). Don't recreate `Source/`.

---

## 9. End note

Cycle 5.2 v0.6 + cycle 5.3 audit are SHIPPED to `origin/main` (`69ae7ee`). GH Issue #1 (branch protection rule) is FILED and gated on first Option-B green roundtrip. Pending reviewer follow-ups (5) are non-blocking and can be bundled into a cycle-5.3.1 polish commit whenever convenient.

The next-session entry point is **§4 Resume protocol** (read order) → **§3 Pending actions** (pick one) → **§3 entry-specific go-do**.

*End of handoff. Tree is clean. HEAD `69ae7ee` synced. Resume whenever ready.*
