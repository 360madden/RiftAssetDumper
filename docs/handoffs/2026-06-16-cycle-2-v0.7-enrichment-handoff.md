# Cycle 2 v0.7 Enrichment Handoff

**Date:** 2026-06-16
**Session:** Autonomous (DeepSeek V4 Pro)
**Phase:** Post-Cycle 2 enrichment — geometry population + material inference

---

## Deliverables

### v0.6 — Geometry Enrichment

- `build_geometry()` now populates `vertex_count`, `face_count`, `has_faces`, `mesh_block`, `mesh_size`, `render_class`, `obj_sha1` from flythrough-index
- Fixed critical bug: SHA-256 → SHA-1 for schema compliance
- Fixed `mesh_block_raw` empty-string guard
- Updated module docstring for accuracy
- Results: 155 faced, 62 point-only, 217/217 with obj_sha1 and M#N mesh_block

### v0.7 — Material Inference

- `build_materials()` infers `material_status` from flythrough texture linkage:
  - linked_textures non-empty → `"textured"` (212 assets)
  - faced but no linked_textures → `"material-or-vertex-color-only"` (2 assets)
  - otherwise → `"unknown"` (3 assets)
- `build_manifest()` passes `flythrough_entry` to `build_materials()`
- `notes` field documents inference provenance honestly
- Results: **153/217 (70.5%) consumer_ready** — up from 0/217

### Validation Suite Updates

- Guard version check: v0.5 → v0.7
- Test pin: `test_all_stage6_producer_version_is_v0_7`
- Rebuilt all 24 stage2 sample manifests with v0.7
- Rebuilt all 217 stage6 scale-out manifests with v0.7
- Regenerated aggregate pack (24 entries, dedup intact)
- Re-ran ingestion test (24/24 all-pass, 144/144 checks)

### Ship-Kill Brief

- Updated `docs/roadmap/cycle-2-briefs/block-4-ship-kill-brief.md` with v0.7 enrichment evidence
- Decision remains SHIP with enhanced metrics

---

## Fix Log

| Issue | Resolution |
|-------|-----------|
| SHA-256 produced 64-char hash but schema expects 40-char SHA-1 | Changed to `hashlib.sha1()` |
| `mesh_block_raw` guard missed empty string | Changed `is not None` → truthiness check |
| Guard version check hardcoded v0.5 | Updated to v0.7 |
| Stage2 samples stale at v0.5 | Rebuilt all 24 with v0.7 |
| Test `test_all_stage6_producer_version_is_v0_5` | Renamed to `_v0_7` and pinned to v0.7 |

---

## Script Inventory

| Script | Version | Purpose |
|--------|---------|---------|
| `scripts/build_scene_manifest.py` | v0.7 | Builder with geometry population + material inference |
| `scripts/build_aggregate_stats.py` | — | Pack builder + dedup |
| `scripts/build_ingestion_test.py` | — | Consumer ingestion validator |
| `scripts/rift_workflow_guards.py` | updated | Guard version pin → v0.7 |
| `tests/test_scene_manifest_validation.py` | updated | Version test pin → v0.7 |
| `tests/test_build_scene_manifest.py` | unchanged | 16 tests, all pass |

---

## Consumer Contract Status

| Condition | Satisfied |
|-----------|:---:|
| vertex_count > 0 | 217/217 |
| face_count > 0 | 155/217 |
| has_faces = true | 155/217 |
| material_status != "unknown" | 214/217 |
| textures.source != "unknown" | 212/217 |
| linked_texture_count > 0 | 212/217 |
| mesh_block != None | 217/217 |
| **All conditions met** | **153/217** |

---

## Forward Reference

- NIF-level material property scan would replace inferred material_status with real data
- 2 faced-no-texture assets need texture linkage investigation
- 62 point-only assets may need triangle-fan fallback face generation
- RiftFlythrough consumer can now adopt scene-manifest-pack-v1.json for 153 assets
