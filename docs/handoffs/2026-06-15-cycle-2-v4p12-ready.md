# Cycle 2 — C2-2.4/C2-2.5 V4P12-Ready Handoff

**Date**: 2026-06-15  
**Status**: C2-V4P12 ready; no V4 Pro session used yet  
**Active lane**: consumer visual fidelity via scene-manifest/world reconstruction

---

## Bottom line

C2-2.4 and C2-2.5 are complete enough for the C2-V4P12 review boundary.
The next action is **C2-V4P12**: review and lock transform truth, coordinate
contract, and scene-manifest v1 schema direction before material closure.

---

## What changed

| Area | Change |
|---|---|
| Cohort drift | Fixed `scripts/build_cycle_2_cohort.py` default output path so the documented `Assets/Exports/.../cohort.json` path is refreshed, not the stale top-level ignored path. |
| Transform evidence | Added `scripts/build_cycle_2_transform_examples.py` to regenerate C2-2.1 transform evidence from the current v0.3 cohort. |
| C2-2.4 docs | Added `stage2/coordinate-contract.md` and `stage2/schema-sketch.md`. |
| Draft schema | Added `stage2/scene-manifest-v1.draft.schema.json` as a review draft, not the locked v1 schema. |
| Schema gate | Added `scripts/cycle_2_schema_validate.py` to meta-validate Draft 2020-12 schemas and optional instances. |
| V4P12 brief | Added `stage2/combined-brief.md` and durable template `docs/roadmap/cycle-2-briefs/block-1-transform-schema.md`. |
| Plan state wiring | Updated `scripts/cycle_2_plan.py` for v0.3 ordering and the combined `C2-V4P12` gate. |

---

## Current evidence

Refreshed `stage2/transform-examples.json` now reports:

| Signal | Result |
|---|---:|
| Cohort size | 26 |
| Available transforms | 26/26 |
| Non-identity transforms | 4 |
| Identity transforms | 22 |
| Full flythrough scale check | 217/217 scale = 1.0 |
| All fields finite | yes |

Transform contract draft:

- truth model: mesh `ParentNiNodeIndex` parent-chain accumulation;
- authoritative placement source: per-asset `world.json` / `scene-graph/v1`;
- composition: `v_world = R * (S * v_local) + T`;
- rotation: 3x3 row-major matrix;
- translation: `[x, y, z]`;
- scale: uniform float;
- identity tolerance: `1e-6`.

---

## Validation run

| Check | Result |
|---|---|
| `python scripts/build_cycle_2_cohort.py` | PASS; wrote 26-entry v0.3 cohort |
| `python scripts/build_cycle_2_transform_examples.py` | PASS; wrote 26/26 transform evidence |
| `python scripts/cycle_2_schema_validate.py Assets/Exports/discovery-plan/cycle-2/stage2/scene-manifest-v1.draft.schema.json` | PASS; valid Draft 2020-12 schema |
| `python scripts/cycle_2_plan.py status` | PASS; next step is `C2-V4P12` |

Full pytest/ruff/mypy is still pending for the final commit gate.

---

## V4P12 handoff

Use:

```text
docs/roadmap/cycle-2-briefs/block-1-transform-schema.md
```

Expected V4P12 output: one concise decision doc with three binding decisions:

1. transform truth model;
2. coordinate contract;
3. scene-manifest v1 field set.

No material-closure work should start until V4P12 returns or the user decides
to skip the stronger-review gate.

---

## Dynamic effort-routing note

Use faster/mechanical execution for regeneration, formatting, and routine
validation. Keep stronger reasoning for schema truth, transform semantics,
material closure classification, cross-repo RiftFlythrough validation, and any
commit/push gate.
