# M3.1 Decision Record — NiDataStream Descriptor Field Read + Classification

**Date**: 2026-06
**Type**: Parser Promotion Decision Record — Phase 3 M3.1
**Status**: **M3.1 COMPLETE** — decision record implemented and verified
**Template**: `docs/nidatastream-parser-export-promotion-decision-template.md`
**Parent(s)**: `docs/handoffs/2026-06-m2.5-phase2-exit-consolidation.md` (Phase 2 exit), `docs/roadmap/phase3-prep.md`
**Roadmap Reference**: Phase 3 M3.1. Candidate-only. Both promotion flags must remain false.

**Note**: This decision record follows the structured template from `docs/nidatastream-parser-export-promotion-decision-template.md`. It documents the formal decision process for the first Phase 3 parser change.

## Implementation Summary (June 2026)

**All planned changes implemented and verified:**

- `NifDataStreamLayout` record: added `string? DescriptorBytes` field
- `AnalyzeNifDataStreamLayout`: extracts 4 bytes at offset 24 as hex (all 3 return paths populated)
- `NifMeshBoundStreamSummary` record: added `string? DescriptorBytes` and `string? DescriptorClassification`
- Both construction sites pass descriptor bytes through and call `ClassifyNifDescriptor()`
- `ClassifyNifDescriptor()` method: maps 5 known patterns (`37 04 03 00`, `36 04 02 00`, `15 02 01 00`, `10 01 04 00`, `3c 01 04 00`), returns null for unknowns
- `FormatNifDescriptorClassificationInline()` helper for console output
- `ProbeNifMesh` console output shows `desc=...` inline after role/confidence

**Verification:**

- Mesh #7 anchor probe: `@212` = ror1-float, `@220` = ror1-float, `@296` = unknown-role, `@304` = float-vertex-data ✅
- Mesh #34 anchor probe: `@212` = ror1-float, `@220` = ror1-float, `@296` = unknown-role, `@304` = float-vertex-data ✅

**CI:** build 0 errors, 17/17 tests pass, format PASS, ruff 0, mypy 0

**Safety:** both promotion flags remain false; all 6 gates still blocked; candidate-only informational

---

---

## Decision Summary

- **Date**: June 2026
- **Proposed parser/export change**: Add `DescriptorBytes` field (4 bytes at offset 24) to NiDataStream record; implement `ClassifyDescriptor()` method that matches against 5 known patterns (`37_04_03_00`, `36_04_02_00`, `15_02_01_00`, `10_01_04_00`, `3c_01_04_00`) and logs classification. No behavioral change to decode/export paths.
- **Scope**: ~50 lines in `Program.cs` — new field + classification method + log output
- **Files changed**: `src/RiftAssetDumper/Program.cs` (NiDataStream record + classification), `src/RiftAssetDumper.Tests/BasicTests.cs` (new targeted tests)
- **Promotion owner/reviewer**: Autonomous agent workflow (Phase 3)
- **Decision**: `candidate-only` — no promotion flags change; classification is informational only

---

## Required Current-Truth Commands

### `nidatastream-promotion-status --list-json` (June 2026)

| Flag | Value |
|---|---|
| `ParserExportPromotionAllowed` | **false** |
| `FieldOrderPromoted` | **false** |

**6 blockers, all in force**:

| Blocker | State |
|---|---|
| `descriptor-field-order-proof` | candidate |
| `descriptor-semantic-map` | blocked |
| `descriptor-table-sample-proof` | candidate |
| `sample-byte-agreement` | candidate |
| `pairing-impact-proof` | candidate |
| `narrow-parser-patch` | blocked |

### `nidatastream-descriptor-proof-status --list-json` (June 2026)

| Item | Status |
|---|---|
| Byte-0 | Mapped as static descriptor-table index |
| Bytes 1-3 | Unmapped for parser/export |
| Helper argument use | Confirmed (helpers mask to byte-0 for 12-byte table lookup; bytes 1-2 unused in tracked helpers) |
| Blockers | `descriptor-record-bytes-1-3-unmapped`, `descriptor-record-bytes-1-2-unmapped-for-parser-export` |

### `nidatastream-layout --root Extracted --full` (previously run, reused)

184 blocks analyzed, 100% Ghidra-style-valid, 8 files, 5 descriptor patterns confirmed.

### `generated-output-guard` (implicit)

All generated outputs under ignored `Exports/`; no Source/Extracted/Exports content staged or committed. ✅

---

## Evidence Gates

| Gate | Required Answer | Evidence Summary | Pass? |
|---|---|---|---|
| **FunctionSite targets** | Are required Ghidra reports/summaries present? | N/A — this change does NOT consume Ghidra evidence. It reads 4 bytes at a structural offset (24) that is independently proven by NiDataStream layout report (184/184 blocks). | ✅ N/A |
| **Descriptor field order** | Which exact bytes/fields prove descriptor order/count/format/component semantics? | **Structural**: 4 bytes at NiDataStream block header offset 24, confirmed by 184-block ShiftedSamples corpus. **Semantics**: Byte-3 = 0x00 universal (confirmed 184/184). Byte-0 = stream-type enumerator (5 observed values). Bytes 1-2 = unmapped (hypothesis: element width / component count but not proven). This change reads the 4 bytes; it does NOT interpret bytes 1-2. | ⚠️ Structural only; semantics incomplete |
| **Sample-byte agreement** | Which copied/extracted sample corpus proves the parser interpretation? | 184-block ShiftedSamples corpus (8 files) from `nidatastream-layout`. 5 patterns documented. Byte-3=0x00 universal. Internal consistency confirmed. Coverage: 0.58% of full copied set (184/31,777). | ⚠️ Internal consistency at sample scale; population-scale validation pending |
| **Pairing impact** | Does the change improve useful position/normal/UV evidence without promoting noise? | **No impact on pairing.** Classification is informational — it labels streams with their descriptor pattern but does NOT change how bindings are routed. Existing pairing logic is unaffected. The pattern labels may eventually assist pairing analysis but no behavioral change is proposed. | ✅ No pairing impact |
| **Parser isolation** | Is the parser patch narrow and covered by targeted tests? | **Narrowest possible change**: read 4 bytes at a known structural offset, match against 5 known patterns, log result. Test plan: 1) probe known NIFs with pattern `37 04 03 00`, verify classification output; 2) verify pattern `36 04 02 00` classification; 3) verify unknown pattern degrades gracefully. | ✅ Narrow + testable |
| **Export isolation** | Is export still blocked until parser truth is validated? | **Export unchanged.** No decode/export path reads the new field. Export is gated by `--experimental-position-source` and `--export-obj` flags, neither of which consume descriptor bytes. | ✅ Export isolated |
| **Generated-output safety** | Are copied/generated assets and reports ignored/not staged? | All generated outputs under ignored `Exports/`; no Source/Extracted/Exports content staged or committed. Classification output is log-level only (no new files). `generated-output-guard` passing throughout Phase 2/3. | ✅ Clean |

---

## Required Negative Checks

| Check | Status | Evidence |
|---|---|---|
| `ParserExportPromotionAllowed` not changed to true | ✅ **Confirmed** | Remains false; no schema or code changes to promotion flags |
| `FieldOrderPromoted` not changed to true | ✅ **Confirmed** | Remains false; descriptor proof still incomplete (bytes 1-3 unmapped) |
| Existing negative fixtures still fail closed | ✅ **Confirmed** | No existing tests modified; no promotion guard weakened |
| `nidatastream-parser-export-non-consumption-guard` updated only alongside deliberate promotion tests | ✅ **Confirmed** | Guard unchanged; new field is informational, not consumed by decode/export |
| No generated reports, copied RIFT assets, or extraction output staged | ✅ **Confirmed** | All under ignored `Exports/` |

---

## Parser/Export Test Plan

- **Targeted parser tests**:
  1. `ClassifyDescriptor_Returns_Ror1Float_For_37_04_03_00` — verify pattern matching
  2. `ClassifyDescriptor_Returns_FloatVertexData_For_36_04_02_00` — verify pattern matching
  3. `ClassifyDescriptor_Returns_Unknown_For_UnrecognizedPattern` — graceful degradation
  4. `ClassifyDescriptor_Logs_Warning_When_Byte3_NonZero` — integrity check (warning only, no crash)
  5. `DescriptorBytes_Read_At_Offset_24` — verify structural read position

- **Targeted export tests**: None — no export behavior change

- **Regression tests for legacy behavior**: All 6 existing xUnit tests must continue to pass

- **Expected generated-output locations**: None — classification is log/in-memory only

- **Commands run**:

  ```bash
  dotnet build RiftAssetDumper.slnx --nologo
  dotnet test RiftAssetDumper.slnx --nologo
  dotnet format RiftAssetDumper.slnx --verify-no-changes
  ```

---

## Rollback Plan

- **Revert commit(s)**: Remove `DescriptorBytes` field and `ClassifyDescriptor()` method from `Program.cs`; remove targeted tests
- **Restore schema locks**: N/A — no schema changes
- **Re-run preflight**: `nidatastream-promotion-status --list-json` — verify 6 blockers unchanged
- **Re-run generated-output guard**: Verify no new files in `Exports/`

---

## Final Decision

- **Approved scope**: NiDataStream record gains `DescriptorBytes` (byte[4]) field read at block header offset 24; `ClassifyDescriptor()` method with 5 known patterns; log-level classification output. **No behavioral change to decode/export paths.**
- **Remaining blockers**: All 6 promotion gates still blocked. `FieldOrderPromoted` = false. `ParserExportPromotionAllowed` = false. Bytes 1-2 semantics unknown. 3/5 descriptor patterns role-unverified. Sample coverage = 0.58%.
- **Next action**: Implement the change in `Program.cs` with targeted xUnit tests. Decision record approved at `candidate-only` level.

---

## Phase 2 Evidence Foundation (for this decision)

| Finding | Source | Relevance to this change |
|---|---|---|
| Descriptor is 4 bytes at offset 24 (per-block embedded) | M2.1 layout report (184/184 blocks) | Structural foundation for the read |
| 5 known patterns with counts | M2.1 + M2.3 | Classification lookup table |
| `37 04 03 00` = ror1-float (position/normal/UV) | M2.2 + M2.3 binding proofs | Verified semantic for pattern matching |
| `36 04 02 00` = float-vertex-data category | M2.3 post-exit evidence (Usage/Access match) | Category-identified semantic |
| Byte-3 = 0x00 universal (184/184) | M2.1 ShiftedSamples | Integrity check (warning only) |
| Both promotion flags false; 6 gates blocked | M2.4 per-gate evaluation | Safety: no overreach |
| Usage/Access determines role, not descriptor | M2.3 unified mapping | Design constraint: classification is informational only |

---

See `docs/roadmap/phase3-prep.md`, `docs/handoffs/2026-06-m2.5-phase2-exit-consolidation.md`, `docs/nidatastream-parser-export-promotion-decision-template.md`.

**End of M3.1 decision record.**
